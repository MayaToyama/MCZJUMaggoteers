# The Maggoteers — Plan 9: Paper 26.2 + MGC 1.0.7 升级 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 Maggoteers 构建与运行基线对齐本地测试服 `E:\MCpaper`（Paper **26.2-65**）及 [MCZJUGameCore **1.0.7**](https://github.com/mczju-ops/MCZJUGameCore)；**不安装 ItemCreator**（决策 C），允许启动 warning 与局内物品缺失。

**Architecture:** 先改 Maven/`plugin.yml`/文档坐标 → 引入 `GameRegistries` 集中解析 Paper Registry API → 按 `mvn compile` 修 `VoidGenerator`/结构粘贴等剩余报错 → 部署到 `E:\MCpaper\plugins` 做冒烟 QA。MGC 生命周期与菜单接入保持不变。

**Tech Stack:** Paper API `26.2.build.62-beta`，Java **25**，Maven，MCZJUGameCore **1.0.7**（jitpack），MCZJUItemCreator **1.0.1**（provided，仅编译；测试服不装）。

**设计 spec：** `docs/superpowers/specs/2026-07-24-paper-26.2-upgrade-design.md`

## Global Constraints

- **测试服路径：** `E:\MCpaper`（jar：`E:\MCpaper\plugins\`；MGC 房间：`E:\MCpaper\plugins\MCZJUGameCore\rooms\maggoteers\default.json`）。
- **测试服 Paper：** `paper-26.2-65.jar`（见 `E:\MCpaper\start.bat`）。
- **ItemCreator：** 测试服**不部署**；保留 `softdepend`；不阻塞 join/波次/菜单/结算 QA。
- **MGC 权威 API：** GitHub tag **1.0.7**（勿用本地过时 clone 1.0.0）。
- **铁律①②③** 继承。
- **构建：** Windows 无 `mvn` 时用 WSL：`wsl bash -lc 'cd /mnt/e/Intellij_Idea/plugins/MCPlugin && mvn …'`。

---

## Task 0: 测试服依赖就位（部署，非代码）

**Files:**
- Deploy: `E:\MCpaper\plugins\MCZJUGameCore-*.jar`（**1.0.7**）
- Ensure: `E:\MCpaper\plugins\MCZJUGameCore\rooms\maggoteers\default.json`（`{}` 即可；可由 Maggoteers `onEnable` 写出）

**Interfaces:**
- Produces: 可启动的 Paper 26.2 服 + MGC 1.0.7，**无** ItemCreator。

- [ ] **Step 1: 获取 MGC 1.0.7 jar**

从 GitHub Releases / tag `1.0.7` 下载或本地构建：

```bash
git clone https://github.com/mczju-ops/MCZJUGameCore.git
cd MCZJUGameCore
git checkout 1.0.7
mvn -q -DskipTests package
```

复制 `target/MCZJUGameCore-1.0.7.jar` → `E:\MCpaper\plugins\`

- [ ] **Step 2: 确认测试服 Paper 与 Java**

```powershell
Get-Content E:\MCpaper\version_history.json
E:\MCpaper\start.bat
```

Expected: `currentVersion` 含 `26.2`；控制台 Java 为 **25**。

- [ ] **Step 3: 勿装 ItemCreator**

确认 `E:\MCpaper\plugins\` **没有** `MCZJUItemCreator*.jar`（决策 C）。

---

## Task 1: Maven + plugin.yml 坐标升级

**Files:**
- Modify: `pom.xml`
- Modify: `src/main/resources/plugin.yml`

**Interfaces:**
- Produces: 项目以 Paper 26.2 + MGC 1.0.7 + JDK 25 解析依赖（compile 可能仍失败，Task 2 修）。

- [ ] **Step 1: 更新 `pom.xml`**

替换/对齐以下片段（其余保持不变）：

```xml
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>25</maven.compiler.release>
  </properties>
```

```xml
    <dependency>
      <groupId>io.papermc.paper</groupId>
      <artifactId>paper-api</artifactId>
      <version>26.2.build.62-beta</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>com.github.mczju-ops</groupId>
      <artifactId>MCZJUGameCore</artifactId>
      <version>1.0.7</version>
      <scope>provided</scope>
    </dependency>
```

`maven-compiler-plugin` 内 `<release>25</release>`。

- [ ] **Step 2: 更新 `plugin.yml`**

```yaml
api-version: '1.21'
```

> Paper 26.2 仍兼容 Bukkit `api-version` 主版本字符串；若启动报 api-version 不匹配，改为服文档要求的值（以 Paper 日志为准）。MGC 1.0.7 的 `plugin.yml` 仍写 `1.21.7`，子插件可与 MGC 对齐为 `1.21` 或 `1.21.7`——**以能加载为准**。

- [ ] **Step 3: 试编译（记录错误清单）**

```bash
wsl bash -lc 'cd /mnt/e/Intellij_Idea/plugins/MCPlugin && mvn -q compile 2>&1 | tail -50'
```

Expected: 可能失败；把错误贴到 Task 2/3 对照表。

- [ ] **Step 4: Commit**

```bash
git add pom.xml src/main/resources/plugin.yml
git commit -m "chore(plan9): bump Paper 26.2, MGC 1.0.7, JDK 25"
```

---

## Task 2: `GameRegistries` — 药水 / 属性 / 实体类型解析

**Files:**
- Create: `src/main/java/io/mczju/maggoteers/util/GameRegistries.java`
- Test: `src/test/java/io/mczju/maggoteers/util/GameRegistriesTest.java`

**Interfaces:**
- Produces:
  - `PotionEffectType GameRegistries.potionEffect(String name)` — `POISON`、`STRENGTH` 等配置名；未知返回 null。
  - `Attribute GameRegistries.attribute(String name)` — `ATTACK_DAMAGE`、`MAX_HEALTH` 等；未知返回 null。
  - `EntityType GameRegistries.entityType(String name)` — `ZOMBIE` 等；未知抛 `IllegalArgumentException`（与 `EntityType.valueOf` 行为一致）。

- [ ] **Step 1: 写失败单测**

```java
package io.mczju.maggoteers.util;

import org.bukkit.entity.EntityType;
import org.bukkit.potion.PotionEffectType;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class GameRegistriesTest {
    @Test
    void potionEffect_resolvesPoison() {
        assertEquals(PotionEffectType.POISON, GameRegistries.potionEffect("POISON"));
    }

    @Test
    void potionEffect_unknownReturnsNull() {
        assertNull(GameRegistries.potionEffect("NOT_A_REAL_POTION_XYZ"));
    }

    @Test
    void entityType_resolvesZombie() {
        assertEquals(EntityType.ZOMBIE, GameRegistries.entityType("ZOMBIE"));
    }
}
```

- [ ] **Step 2: 跑测确认失败**

```bash
wsl bash -lc 'cd /mnt/e/Intellij_Idea/plugins/MCPlugin && mvn -q test -Dtest=GameRegistriesTest 2>&1 | tail -20'
```

Expected: FAIL（类不存在）。

- [ ] **Step 3: 实现 `GameRegistries.java`**

```java
package io.mczju.maggoteers.util;

import org.bukkit.NamespacedKey;
import org.bukkit.Registry;
import org.bukkit.attribute.Attribute;
import org.bukkit.entity.EntityType;
import org.bukkit.potion.PotionEffectType;

import java.util.Locale;
import java.util.Map;

/** Paper 26.x Registry 解析（替代 getByName / Attribute.valueOf / EntityType.valueOf）。 */
public final class GameRegistries {

    private static final Map<String, String> ATTRIBUTE_LEGACY = Map.ofEntries(
            Map.entry("MAX_HEALTH", "generic.max_health"),
            Map.entry("ATTACK_DAMAGE", "generic.attack_damage"),
            Map.entry("MOVEMENT_SPEED", "generic.movement_speed"),
            Map.entry("ATTACK_SPEED", "generic.attack_speed")
    );

    private GameRegistries() {}

    public static PotionEffectType potionEffect(String name) {
        if (name == null || name.isBlank()) return null;
        String u = name.trim().toUpperCase(Locale.ROOT);
        PotionEffectType direct = Registry.POTION_EFFECT_TYPE.get(NamespacedKey.minecraft(u.toLowerCase(Locale.ROOT)));
        if (direct != null) return direct;
        try {
            return Registry.POTION_EFFECT_TYPE.get(NamespacedKey.minecraft(
                    u.toLowerCase(Locale.ROOT).replace('_', '.')));
        } catch (Exception e) {
            return null;
        }
    }

    public static Attribute attribute(String name) {
        if (name == null || name.isBlank()) return null;
        String u = name.trim().toUpperCase(Locale.ROOT);
        String key = ATTRIBUTE_LEGACY.getOrDefault(u, u.toLowerCase(Locale.ROOT).replace('_', '.'));
        return Registry.ATTRIBUTE.get(NamespacedKey.minecraft(key));
    }

    public static EntityType entityType(String name) {
        if (name == null || name.isBlank()) throw new IllegalArgumentException("entity type blank");
        String u = name.trim().toUpperCase(Locale.ROOT);
        EntityType t = Registry.ENTITY_TYPE.get(NamespacedKey.minecraft(u.toLowerCase(Locale.ROOT)));
        if (t == null) throw new IllegalArgumentException("Unknown entity type: " + name);
        return t;
    }
}
```

> **实现注意：** 若 `Registry.POTION_EFFECT_TYPE` / `Registry.ATTRIBUTE` 在 26.2 上符号不同，以 `mvn compile` 为准改 import（常见为 `org.bukkit.Registry` + `io.papermc.paper.registry.RegistryAccess`）。单测只断言 POISON/ZOMBIE；编译通过即达标。

- [ ] **Step 4: 跑测通过**

```bash
wsl bash -lc 'cd /mnt/e/Intellij_Idea/plugins/MCPlugin && mvn -q test -Dtest=GameRegistriesTest'
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/main/java/io/mczju/maggoteers/util/GameRegistries.java \
        src/test/java/io/mczju/maggoteers/util/GameRegistriesTest.java
git commit -m "feat(plan9): GameRegistries for Paper 26.2 registry APIs"
```

---

## Task 3: 替换全项目 deprecated 解析调用

**Files:**
- Modify: `src/main/java/io/mczju/maggoteers/reward/RewardOption.java`（`parseParams` potion + `safeAttribute`）
- Modify: `src/main/java/io/mczju/maggoteers/mob/MobFactory.java`（`applyAffixPotions`）
- Modify: `src/main/java/io/mczju/maggoteers/listener/CombatAffixListener.java`
- Modify: `src/main/java/io/mczju/maggoteers/config/WavesConfig.java`（`EntityType.valueOf` → `GameRegistries.entityType`）

**Interfaces:**
- Consumes: `GameRegistries.potionEffect` / `attribute` / `entityType`

- [ ] **Step 1: `RewardOption`**

`case "potion"` 内：

```java
PotionEffectType p = io.mczju.maggoteers.util.GameRegistries.potionEffect(String.valueOf(v));
```

`safeAttribute`：

```java
private static Attribute safeAttribute(Object v) {
    return io.mczju.maggoteers.util.GameRegistries.attribute(String.valueOf(v));
}
```

- [ ] **Step 2: `MobFactory.applyAffixPotions`**

```java
var type = io.mczju.maggoteers.util.GameRegistries.potionEffect(ps.effect());
```

- [ ] **Step 3: `CombatAffixListener`**

```java
PotionEffectType type = io.mczju.maggoteers.util.GameRegistries.potionEffect(ps.effect());
```

- [ ] **Step 4: `WavesConfig.parseStrategy`**

```java
EntityType type = io.mczju.maggoteers.util.GameRegistries.entityType(String.valueOf(m.get("type")));
```

- [ ] **Step 5: 全量编译 + 单测**

```bash
wsl bash -lc 'cd /mnt/e/Intellij_Idea/plugins/MCPlugin && mvn -q test'
```

Expected: BUILD SUCCESS（若仍有错误 → Task 4）

- [ ] **Step 6: Commit**

```bash
git add src/main/java/io/mczju/maggoteers/reward/RewardOption.java \
        src/main/java/io/mczju/maggoteers/mob/MobFactory.java \
        src/main/java/io/mczju/maggoteers/listener/CombatAffixListener.java \
        src/main/java/io/mczju/maggoteers/config/WavesConfig.java
git commit -m "refactor(plan9): use GameRegistries instead of getByName/valueOf"
```

---

## Task 4: 编译剩余项（VoidGenerator / Structure / 其他）

**Files:**
- Modify: `src/main/java/io/mczju/maggoteers/world/VoidGenerator.java`（若 `ChunkGenerator` 签名变更）
- Modify: `src/main/java/io/mczju/maggoteers/world/StructurePaster.java`（若 `Structure` API 变更）
- Modify: 任意 `mvn compile` 仍报错的文件

**Interfaces:**
- Consumes: Paper 26.2 javadoc / 编译器错误信息

- [ ] **Step 1: 修 `VoidGenerator`**

若报错 `shouldGenerate*` 无参方法 removed：改为 Paper 26.2 要求的带 `WorldInfo`/`GeneratorSettings` 的重载，或删除已无用的 override（**保持虚空语义**：不生成地形）。

参考前代约束（Plan 1）：所有 `shouldGenerate*()` 返回 false；`generateSurface` 空实现。

- [ ] **Step 2: 修 `StructurePaster`**

若 `loadStructure`/`place` 签名变化：按编译器提示改参数顺序/类型；**禁止**改懒粘贴策略（Act 进层再粘）。

- [ ] **Step 3: `mvn test` 全绿**

```bash
wsl bash -lc 'cd /mnt/e/Intellij_Idea/plugins/MCPlugin && mvn -q test'
```

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "fix(plan9): Paper 26.2 API adjustments (world/structure)"
```

---

## Task 5: 文档版本基线

**Files:**
- Modify: `CLAUDE.md`（§1.2 技术栈、§1.3 依赖坐标、Paper javadoc 链接、MGC 基线 **1.0.7**、JDK **25**）
- Modify: `docs/dev-log.md`（追加 Plan 9 条目：26.2 升级 + ItemCreator C + 测试服路径）

**Interfaces:**
- Produces: 文档与代码一致

- [ ] **Step 1: 更新 `CLAUDE.md` 关键行**

| 字段 | 新值 |
|---|---|
| Paper | 26.2（API `26.2.build.62-beta`） |
| Java | 25 |
| MGC 设计基线 | **1.0.7** |
| javadoc | `https://jd.papermc.io/paper/26/`（或当前 Paper 文档根路径） |
| 测试服 | `E:\MCpaper` |

- [ ] **Step 2: `dev-log` 顶条**

记录：Plan 9 动机（服已 26.2-65）、MGC 1.0.7、ItemCreator 暂不装、遗留 in-game QA。

- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md docs/dev-log.md
git commit -m "docs(plan9): baseline Paper 26.2 + MGC 1.0.7 + JDK 25"
```

---

## Task 6: 打包部署 + 冒烟 QA（`E:\MCpaper`）

**Files:**
- Deploy: `E:\MCpaper\plugins\Maggoteers-0.1.0-SNAPSHOT.jar`
- Optional clean: `E:\MCpaper\plugins\Maggoteers\` 下旧 yml（若需刷新默认配置）

**Interfaces:**
- Consumes: Task 0 MGC jar + Task 1–4 构建产物

- [ ] **Step 1: 打包**

```bash
wsl bash -lc 'cd /mnt/e/Intellij_Idea/plugins/MCPlugin && mvn -q -DskipTests package'
```

- [ ] **Step 2: 复制 jar**

```powershell
Copy-Item "E:\Intellij_Idea\plugins\MCPlugin\target\Maggoteers-0.1.0-SNAPSHOT.jar" "E:\MCpaper\plugins\"
```

- [ ] **Step 3: 启动服并看日志**

```powershell
E:\MCpaper\start.bat
```

Expected 日志片段：

- `Maggoteers (卫戍协议) enabled`
- `MCZJUItemCreator 未安装` → **warning 可接受**
- `RewardService` 可能有 unknown item **warning**
- **无** `IllegalStateException`（waves 池/strategy 校验）

- [ ] **Step 4: 游戏内冒烟（ItemCreator 缺失下）**

| 步骤 | 预期 |
|---|---|
| `/mgc join maggoteers` | 能进等待/开局（需 `rooms/maggoteers/default.json`） |
| 满员/ min 玩家开局 | 传送、波次启动 |
| 清一波 | 休整菜单可开（STAT 奖励仍可用） |
| 死亡/复活 | 不崩服 |
| `/maggoteers shop` | 局外菜单可开（无物品 icon 降级可接受） |
| 通关或 `/maggoteers debug state` | 不 NPE |

- [ ] **Step 5: 勾选本 plan 验收 + dev-log 遗留**

---

## Plan 9 Definition of Done

- [ ] `mvn test` 绿；`mvn package` 成功。
- [ ] `pom.xml`：Paper `26.2.build.62-beta`，MGC `1.0.7`，JDK 25。
- [ ] `E:\MCpaper\plugins\` 含 MGC 1.0.7 + Maggoteers jar；**无** ItemCreator。
- [ ] 冒烟 QA 表（Task 6 Step 4）核心项通过。
- [ ] `CLAUDE.md` + `docs/dev-log.md` 已更新。

> **ItemCreator 恢复后：** 另开小任务部署 ItemCreator jar + 复测 WEAPON/SUPPLY/货币外观，不在 Plan 9 范围。
