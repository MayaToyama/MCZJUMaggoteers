# 设计复核 / 接入契约核对清单（给接手 agent）

- **日期**：2026-07-02（第二轮复核，覆盖初版）
- **性质**：进入 `writing-plans` 前的最终核对。**逐条对照 GitHub MGC 1.0.5 源码、测试服 jar、前代原型**，不是凭空推测。
- **版本基线（最高优先级）**：**一切以 GitHub `mczju-ops/MCZJUGameCore` pom `1.0.5` 为准**（`docs/dev-guide.md` + `docs/dev-advanced.md`）。
- **核对基准**：
  | 来源 | 路径 / 版本 | 能否作为契约依据 |
  |---|---|---|
  | **GitHub MGC 1.0.5** | `https://github.com/mczju-ops/MCZJUGameCore`（main，pom `1.0.5`） | ✅ **唯一权威** |
  | 本地源码 clone | `E:\Intellij_Idea\pluginTest\MCZJUGameCore-main` → pom **1.0.0** | ❌ **过时，勿用**（无 PlayerData；Menu API 也不同） |
  | 测试服 jar | `/mnt/e/MCpaper/plugins/MCZJUGameCore-1.0.4.jar` | ⚠️ 参考部署态；**已有 PlayerData**，建议升到 1.0.5 对齐设计 |
  | 前代原型 | `E:\Intellij_Idea\pluginTest\MCZJUvampireSurvivor` | ✅ 世界/波次/缩放借鉴 |
- **怎么用**：§C = 已验证正确、**不要再质疑** → §B/D 中「已整改」= CLAUDE.md 已写入 → §E = **文档仍错、进 plan 前须改** → §F = 实现期注意。**无 Blocker，可进 writing-plans**，但 §E 应先补进文档。

---

## A. ~~Blocker：JsonPlayerData 不存在~~ → **已证伪（勿再当 Blocker）**

> 初版清单基于本地 **MGC 1.0.0** 源码得出「持久化 API 不存在」。**此结论错误。**

**GitHub 1.0.5 确认存在**：
- `MCZJUGameCore.getPlayerDataManager()`（`MCZJUGameCore.java`）
- `registerPlayerData(gameId, Class)`、`JsonPlayerData`、`PlayerExt.getData(T)`、`resetState()`
- `PlayerDataLeaderboard`、`getLeaderboardManager()`（`docs/dev-advanced.md` §三、§四）

**处置**：`CLAUDE.md` §13 持久化方案（`MaggoteersPlayerData extends JsonPlayerData`）**保留，不改为自建持久化**。

**注意**：MGC 1.0.5 **同时有** Profile 系统（背包快照）与 PlayerData 系统（自定义 JSON 字段）——元进度走 **PlayerData**，局内背包切换走 **Profile**，勿混用。

---

## B. API / 生命周期 — 处置汇总

| ID | 初版结论 | 对照 1.0.5 | 处置 |
|---|---|---|---|
| **B1** MenuFacade 签名 | 文档错 | `registerMenu(String menuId, Class<? extends Menu>)` — **与 CLAUDE.md §2.2 一致** | ✅ 原文档正确；初版清单错（那是 1.0.0 的 API） |
| **B2** getData / resetState | 不存在 | `PlayerExt.java` 均存在 | ✅ 原文档正确 |
| **B3** 异步建世界 | 成立 | `createWorld()` 必须主线程；VampireSurvivor 同步建世界、仅删目录异步 | ✅ **已整改** CLAUDE.md §2.3/§4.1/§5.1 |
| **B4** onGameStart 不能阻塞 | 成立 | `gameStart()` 调完立即 `RUNNING` | ✅ **已整改**：就绪门闩（`runTaskTimer` 轮询） |
| **B5** jitpack groupId | 存疑 | `com.github.mczju-ops`；VampireSurvivor `build.gradle` 实证 | ✅ 正确（包名 `mczjuops` 无连字符不影响坐标） |

---

## C. ✅ 已核实正确（1.0.5，不要再质疑）

- `registerGame(Class, Class)`、`DefaultGameWaitStrategy(game, 4, min)` 参数顺序
- 生命周期 hook：`onGameInit / onGameStart / onGameCancel / onGameAbort / onGameEnd`
- `getId()` + `getGameMeta()`（勿用旧 `guide.md` 里的 `getName()`）
- `AbstractPlayerDeathStrategy`、`MidGameJoinable` / `OpenSessionGame`（我们不实现 MidGameJoinable）
- `PlayerExt`：`switchProfile(null)`、`giveItem(id)`、`getGame()`、`getParty()`、`isInGame(Class)`
- `AlertMenu(Player, Runnable)` — 1.0.5 构造器签名为 `Player`，非 `PlayerExt`
- 结构粘贴：`loadStructure(File)` + `structure.place(...)` — 与 VampireSurvivor 一致
- `endGame(game)`、`ScoreManager` 空壳 → 账户货币走 JsonPlayerData 字段（判断正确）
- Effect 5 种口径、`ADD_ATTRIBUTE` 带 `op: PERCENT|FLAT` — 三文档一致

---

## D. 设计层规则 — 处置汇总

| ID | 内容 | CLAUDE.md |
|---|---|---|
| **D1** 净化 0s 255 级凋零抵消存疑 | 实现期实测；fallback = `EntityDamageEvent` cancel | ✅ §10.2/§18 已登记 |
| **D2** CD 勿用 `setCooldown(Material)` | 走 PDC id 时间戳表 | ✅ §10.3 已整改 |
| **D3** 3 选 1 不足 3 个 | 按可见数开格、不足灰显、不补凑 | ✅ §12.2 已整改 |
| **D4** 胜负区分 | 游戏状态存 `outcome` WIN/FAIL | ✅ §13.2 已整改 |
| **D5** 属性多层叠加 | 每层唯一 `NamespacedKey` | ✅ §10.3 已整改 |
| **D6** mob_count 概率补 1 | 必须走 RunPlanner 种子 RNG | ✅ §7.6 已整改 |
| **D7** 触发型被动 stack | 默认 `IGNORE` | ✅ §12.1 已整改 |
| **D8** 结构粘贴掉帧、缩放开局锁定 | 接受，登记风险 | ✅ §18 已登记 |

---

## E. 🟠 文档仍错 / 未写清（进 plan 前须补）

> 以下在 **GitHub 1.0.5 复核**中发现；`CLAUDE.md` 整改轮次**尚未覆盖**。

- [ ] **E1. 持久化路径错误**（§13.1、spec §12）
  - 文档写：`plugins/Maggoteers/players/maggoteers/<uuid>.json`
  - **1.0.5 实际**：`plugins/MCZJUGameCore/player_data/maggoteers/<uuid>.json`（`JsonPlayerData.getFilePath()`）
  - 数据由 MGC 管理，不在 Maggoteers 插件目录。

- [ ] **E2. 自动落盘周期错误**（§13.1）
  - 文档写：每 **5 分钟** + 退出 + 关服
  - **1.0.5 实际**：`PlayerDataManager.startAutoSave(20*60*30L)` = **30 分钟**定时 + 玩家退出 `savePlayerDataAsync` + 关服 `saveAllPlayerData`

- [ ] **E3. 测试服表述需修正**（§1.2、§18）
  - 文档写：测试服旧版「缺 JsonPlayerData API」
  - **实测**：测试服 jar 为 **1.0.4**，已含 `JsonPlayerData` / `PlayerDataManager` / `PlayerDataLeaderboard`；尚无 `player_data/` 目录是因未注册使用过
  - **正确表述**：设计基线 = **1.0.5**；测试服 **1.0.4 → 1.0.5** 升级以对齐设计，而非「缺 API 编译不过」

- [ ] **E4. Menu 1.0.5 构造模式未写**（§2.2、§9.1、§13.3）
  - 1.0.5 `Menu` 子类须：`public XxxMenu(Player player, Object... args) { super(player, args); }`
  - 重写 `getTitle()` / `getRows()` / `getPermission()` / `setup()`
  - `registerMenu` 要求 `(Player, Object...)` 构造器（见 `dev-advanced.md` §一）
  - ⚠️ **勿照抄**本地 1.0.0 `guide.md` 的 `super(ExampleMenu.class, player)` 写法

- [ ] **E5. 排行榜注册未写全**（§13.5）
  - 除继承 `PlayerDataLeaderboard` 外，还须：
    ```java
    MCZJUGameCore.getLeaderboardManager()
        .registerLeaderboard("maggoteers_total", MaggoteersTotalLeaderboard.class);
    ```
  - 子类实现：`getTitle()`、`getSubtitle()`、`getPlayerDataClass()`、`getFieldName()` → `"totalEarned"`
  - 展示实体需 `/mgcop leaderboard create|edit`（`dev-advanced.md` §四）

- [ ] **E6. §2.4 小节交叉引用错误**
  - `JsonPlayerData` 误指 §10.3 → 应为 **§13.1**
  - 排行榜误指 §10.5 → 应为 **§13.5**

- [ ] **E7. spec 未与 CLAUDE.md 同步**（`docs/spec/2026-07-02-maggoteers-design.md`）
  - §4 仍写 `WorldService.createAsync`，未提就绪门闩
  - §9 仍写 `setCooldown`（CLAUDE.md 已改 PDC 时间戳表）
  - §12 持久化路径仍错（同 E1）

- [ ] **E8. RunPlan 仍含 `World` 引用**（§6）
  - `spawnPointAbs{id→World(x,y,z)}` — RunPlanner 异步线程不应持有 `World`
  - 建议改为纯坐标 `{x,y,z}`，主线程绑定 `World` 时再构造 `Location`

---

## F. 实现期注意（不阻塞 plan）

- **本地 clone 陷阱**：`MCZJUGameCore-main` = **1.0.0**；任何 agent 核对 API 必须拉 GitHub **1.0.5**，或读 `dev-advanced.md` 在线版。
- **MGC 文档优先级**：Menu / PlayerData / Leaderboard → **`dev-advanced.md`**；生命周期 / 房间 → **`dev-guide.md`**。旧 `guide.md` 部分示例已过时。
- **ItemCreator** jitpack 版本号、`parseYamlToItems` 等价性 — 仍待实现期核实（spec §14）。
- **净化技巧（D1）** — 实现期实测。

---

## 一句话结论

玩法主体（世界/波次/效果/词缀/缩放）扎实；**初版 §A Blocker 系对照过时 1.0.0 源码的误判**，GitHub **1.0.5 完整提供 JsonPlayerData 持久化与排行榜 API**。B3/B4 与 D1–D8 已在 `CLAUDE.md` 整改。**无 Blocker，可进 writing-plans**；plan 第一阶段应含：测试服 MGC 1.0.4→1.0.5 升级，以及 **§E 八项文档修正**（尤其 E1 路径、E2 落盘周期、E4/E5 接入示例）。

---

## 变更记录

| 轮次 | 对照基准 | 结论 |
|---|---|---|
| 初版 | 本地 MGC **1.0.0** | §A Blocker + §B1/B2 误报 |
| 二轮 | GitHub MGC **1.0.5** + 测试服 **1.0.4** jar | §A/B1/B2/B5 证伪；B3/B4/D* 成立并已写入 CLAUDE.md；新增 §E 文档遗漏 |

---

## §E 处置结论（2026-07-02，已整改入 CLAUDE.md / spec）

| 项 | 整改位置 |
|---|---|
| **E1** 持久化路径 | CLAUDE.md §13.1、spec §12 → `plugins/MCZJUGameCore/player_data/maggoteers/<uuid>.json`（`JsonPlayerData.getFilePath()` 实证） |
| **E2** 落盘周期 | CLAUDE.md §13.1 → **30 分钟**（`startAutoSave(20*60*30L)` 实证）+ 退出/关服 |
| **E3** 版本表述 | CLAUDE.md §1.2/§18、spec §3 → 1.0.4 已含 PlayerData；基线 1.0.5；本地 clone=1.0.0 过时 |
| **E4** Menu 构造 | CLAUDE.md §2.2 → `(Player, Object...){ super(player,args); }` + `getTitle/getRows/getPermission/setup` |
| **E5** 排行榜注册 | CLAUDE.md §13.5 → `registerLeaderboard(id,Class)` + `PlayerDataLeaderboard` 四方法 |
| **E6** 交叉引用 | CLAUDE.md §2.4 → §13.1 / §13.5 |
| **E7** spec 同步 | spec §3/§4/§9/§12 已与 CLAUDE.md 对齐 |
| **E8** RunPlan 去 World | CLAUDE.md §6 → `spawnPointAbs{id→{x,y,z}}` 纯坐标 |

> §E1–E8 全部已整改（源码实证 E1/E2/E5）。**无 Blocker，可进 writing-plans**。
